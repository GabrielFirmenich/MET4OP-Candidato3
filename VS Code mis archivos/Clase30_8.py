prompt = "Ingrese el precio del producto:"
prompt += "/n o `salir` para terminar el programa"

active = True 
while active:
    mensaje = input(prompt)
    if mensaje == `salir`
    active == False 
